package connect.event.equipeMarketing.repository;

import connect.event.equipeMarketing.entity.SegmentAudience;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SegmentAudienceRepository extends JpaRepository<SegmentAudience, Long> {
}
